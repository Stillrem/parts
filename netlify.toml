[build]
  publish = "site"
  functions = "functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["axios","cheerio"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
